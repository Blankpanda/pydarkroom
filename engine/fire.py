from engine import init
class Fire(object):
    def __init__(self):
        self.kindle_count = 0
        self.intensity = 0
        self.INTENSITY_MAXIMUM = 100

    def kindle(self , value):
        intensity += value

        if intensity > INTENSITY_MAXIMUM:
            intensity = INTENSITY_MAXIMUM
            
        kindle_count += 1

    def get_current_intensity():



    def get_intensity_state(intenisty):
        intensity_states = init.get_fire_intensity_states()
        return intensity_states[intenisty]
